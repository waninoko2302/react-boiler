/**
 * A link to a certain page, an anchor tag
 */

import styled from 'styled-components';

const A = styled.div`
  text-align: center;
  display: block;
  font-size: 20px;
  color: green;
`;

export default A;
