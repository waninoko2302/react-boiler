/**
 * A link to a certain page, an anchor tag
 */

import styled from 'styled-components';

const A = styled.span`
  display: block;
  font-size: 50px;
  color: green;
  background-color: black;
`;

export default A;
